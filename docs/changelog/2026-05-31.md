# Changelog — 2026-05-31

## Plano 02 — Fase 1: visual CMD/PowerShell

- **`src/index.css`** — paleta CMD (#0c0c0c, #cccccc, #569cd6, etc.); Consolas; removidos glow verde, scanlines e tokens `terminal-green`
- **`TerminalWindow.tsx`** — barra de título `#1e1e1e`, botões cinza estilo cmd.exe, cantos `rounded-sm`
- **`TerminalPrompt.tsx`** — prompt `PS C:\Users\kiover\portfolio>` (PowerShell)
- **`BootScreen.tsx`** — cores CMD, prompt PowerShell, sem scanlines
- **Componentes** — Navbar, Hero, About, Skills, Projects, Gallery, Contact, Footer, Cursor, TypingText: verde Matrix → cinza/accent/success
- Build `npm run build` OK

## Plano 02 — Fase 2: navegação por terminal

- Recriado **`iniciar-site.bat`** — inicia `npm run dev` na raiz do projeto
- **`NavigationContext.tsx`** — `ViewId`, `navigateTo()`, hash URL (`#projects`, etc.)
- **`TerminalShell.tsx`** — layout navbar + conteúdo + footer
- **`TerminalTransition.tsx`** — overlay com typing do comando ao trocar view
- **`ViewRouter.tsx`** — uma view por vez com `AnimatePresence`
- **`Navbar.tsx`** — botões + `navigateTo` (sem scroll-spy)
- **`Hero.tsx`** — botões internos usam `navigateTo`
- Removido **`useScrollSpy.ts`**
- Build OK

## Ajuste — console PowerShell imersivo

- **`TerminalConsole.tsx`** — janela única com scroll interno (não scroll do site)
- Prompt fixo na base: `PS C:\Users\kiover\portfolio>` digita comando lento (90ms/char)
- Após Enter: comando sobe no histórico, scroll interno, conteúdo troca
- Seções sem `TerminalWindow` duplicado
- Removido overlay `TerminalTransition`
- `body`/`#root` com `overflow: hidden`

## Plano 02 — Fase 3: som de teclado

- **`useKeyboardSound.ts`** — Web Audio API, `localStorage` `terminal-sound`, respeita `prefers-reduced-motion`
- **`SoundToggle.tsx`** — botão `[ som ]` / `[ mudo ]` na barra do terminal
- **`TypingText.tsx`** — callback `onCharTyped` nas transições de nav
- **`BootScreen.tsx`** — sons simulados ao exibir linhas do boot
- **`KeyboardSoundProvider`** em `App.tsx`

## Plano 02 — Fase 4: contato só links

- **`Contact.tsx`** — formulário removido; mailto, Discord, GitHub + `[ copiar discord ]`
- Texto: `mail — use os links abaixo para contato direto`
- Sem webhook, sem POST

## Plano 02 — Fase 5: deploy Discloud

- **`server.js`** — Express estático + SPA fallback, porta 8080, host `0.0.0.0`
- **`discloud.config`** — TYPE=site, ID=kiover, BUILD/START configurados
- **`package.json`** — `express` em dependencies, script `"start": "node server.js"`
- Removido **`vite-plugin-singlefile`** — build gera `dist/` com assets separados
- Build e `node server.js` OK

## Dados — colaboração bot whitelist

- **`projects.ts`** — colaboração **Bot de Whitelist & Advertências** marcada como `concluido`
- Descrição atualizada: colaboração encerrada e concluída
- Link Discord da colaboração removido (projeto finalizado)

## Plano 02 — concluído ✅

- [x] Fase 1–5 implementadas
- [x] Documentação deploy: `docs/deploy/discloud.md`
- [x] Script zip: `preparar-discloud.bat`
- Pendente: upload manual no painel Discloud → `kiover.discloud.app`
